import React from 'react';
import { BaseCardProps } from './types';

export const ActivityModificationCard: React.FC<BaseCardProps> = ({
  courses,
  setCourses,
  classes,
  setClasses,
  showSnackbar
}) => {
  // Ce composant sera implémenté avec les fonctionnalités d'activité
  return (
    <div>
      Activity Modification Card - En cours d&apos;implémentation
    </div>
  );
};
